# Kubernetes Secret Template
# This file is a template. Do NOT commit actual secret values to Git.
# 
# To create the actual secret, run:
# kubectl create secret generic cloudage-secrets \
#   --from-literal=AWS_ACCESS_KEY_ID=your-access-key \
#   --from-literal=AWS_SECRET_ACCESS_KEY=your-secret-key \
#   --namespace=cloudage
#
# Or use AWS IAM Roles for Service Accounts (IRSA) - RECOMMENDED
# This eliminates the need for hardcoded credentials

apiVersion: v1
kind: Secret
metadata:
  name: cloudage-secrets
  namespace: cloudage
  labels:
    app: cloudage-education
type: Opaque
# data:
#   AWS_ACCESS_KEY_ID: <base64-encoded-value>
#   AWS_SECRET_ACCESS_KEY: <base64-encoded-value>
